naftiko: 1.0.0-alpha2
info:
  label: Postmark API — Locations
  description: 'Postmark API — Locations. 1 operations. Lead operation: Postmark Get clicks by body location. Self-contained
    Naftiko capability covering one Postmark business surface.'
  tags:
  - Postmark
  - Locations
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    POSTMARK_API_KEY: POSTMARK_API_KEY
capability:
  consumes:
  - type: http
    namespace: postmark-locations
    baseUri: http://{{baseurl}}
    description: Postmark API — Locations business capability. Self-contained, no shared references.
    resources:
    - name: stats-outbound-clicks-location
      path: /stats/outbound/clicks/location
      operations:
      - name: get
        method: GET
        description: Postmark Get clicks by body location
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: tag
          in: query
          type: string
          description: Filter by tag
        - name: fromdate
          in: query
          type: string
          description: Filter messages starting from the date specified. e.g. `2014-02-01`
        - name: todate
          in: query
          type: string
          description: Filter messages up to the date specified. e.g. `2014-02-01`
  exposes:
  - type: rest
    namespace: postmark-locations-rest
    port: 8080
    description: REST adapter for Postmark API — Locations. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/stats/outbound/clicks/location
      name: stats-outbound-clicks-location
      description: REST surface for stats-outbound-clicks-location.
      operations:
      - method: GET
        name: get
        description: Postmark Get clicks by body location
        call: postmark-locations.get
        with:
          tag: rest.tag
          fromdate: rest.fromdate
          todate: rest.todate
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: postmark-locations-mcp
    port: 9090
    transport: http
    description: MCP adapter for Postmark API — Locations. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: postmark-get-clicks-body-location
      description: Postmark Get clicks by body location
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: postmark-locations.get
      with:
        tag: tools.tag
        fromdate: tools.fromdate
        todate: tools.todate
      outputParameters:
      - type: object
        mapping: $.
